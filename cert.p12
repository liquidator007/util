if [ $# -ne 3 ]
then
  printf "\n\nUso:\n\t$0 <fich pem> <fich. key de la CA> <Nombre a elegir>\n\n"
  printf "(la CA debe estar instalada con c_rehash)\n\n\n"
  exit 1
fi

openssl pkcs12 -export -in $1.pem -inkey $2 -out $1.p12 -name "$3"

