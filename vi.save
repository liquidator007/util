#!/bin/bash

if [ $# -eq 0 ]
then
  printf "\nUso:\n\t$0 file...\n\n"
  exit 1
fi

TS=`date '+%Y-%m-%d-%H-%M-%S'`

for F in $*
do
  if [ -f "${F}" -a -r "${F}" ]
  then
    NF="${F}.${TS}"
    cp "${F}" "${NF}"
    vi "${F}"
  else
    echo $F: doesn't exist or isn't readable
  fi
done
