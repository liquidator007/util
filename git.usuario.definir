#!/bin/bash

if [ $# -ne 2 ]
then
  printf "\n\nUso:\n\t$0 nombre-usuario e-mail...\n\n"
  exit 1
fi

git config --global user.name $1
git config --global user.email $2

#git commit --amend --author='Your Name <you@example.com>'
