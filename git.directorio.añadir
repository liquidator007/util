#!/bin/bash

if [ $# -eq 0 ]
then
  printf "\n\nUso:\n\t$0 directorio...\n\n"
  exit 1
fi

#'git init' crea ".git" en la carpeta de repositorio

for D in $*
do
cd $D && git init && git add . && git commit
done
